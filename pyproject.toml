[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "aeib-receipt-fuzzer"
version = "0.2.0"
description = "Wire-Level Fault Proxy, Toxic Receipt Detector & Offline Audit Log Scanner"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
